spring:
  application:
  #这个地方要与规则一致，配置文件前缀要一样
    name: dm-provider
  cloud:
    config:
      uri: http://localhost:3001/
      profile: default
      label: master
#management:
#  security:
#      enabled: false